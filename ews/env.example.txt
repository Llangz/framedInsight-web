# ============================================================================
# Supabase Configuration (REQUIRED)
# ============================================================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================================================
# App URL (REQUIRED in production)
# ============================================================================
# Used by lib/daraja.ts to build the M-Pesa callback URL, and by
# app/robots.ts / app/sitemap.ts. Must be the public HTTPS URL Safaricom can
# reach. Falls back to https://framed-insight-web.vercel.app if unset, so it's
# only strictly required if you deploy to a different domain.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================================================
# Security (REQUIRED — the app throws a startup error if CSRF_SECRET is unset)
# ============================================================================
# Generate with: openssl rand -base64 32
CSRF_SECRET=

# Authenticates Vercel Cron → GET /api/cron/ews and
# GET /api/cron/reconcile-payments. Generate with: openssl rand -hex 32
CRON_SECRET=

# OPTIONAL — WhatsApp number (E.164, e.g. +2547XXXXXXXX) that
# app/api/cron/reconcile-payments/route.ts pings when a payment's
# subscription activation has failed MAX_ATTEMPTS times and needs a
# manual look in /admin/subscriptions. Leave unset to skip the WhatsApp
# ping — the failure is still recorded in audit_log and surfaced in the
# admin UI either way.
ADMIN_ALERT_PHONE=

# Shared secret for server-to-server calls (send-otp → Supabase Edge Function
# that dispatches the SMS). Generate with: openssl rand -hex 32
INTERNAL_API_SECRET=

# ============================================================================
# Rate limiting — Upstash Redis (RECOMMENDED for production)
# ============================================================================
# lib/security.ts#checkRateLimit() uses these when present and falls back to
# an in-memory limiter when they're absent. The in-memory fallback does NOT
# survive serverless cold starts on Vercel, so set one of these pairs in
# production. Either naming works: KV_* is what Vercel's own "Upstash for
# Redis" Marketplace integration injects automatically; UPSTASH_* is what
# Upstash's own dashboard gives you if you provision the database directly.
KV_REST_API_URL=
KV_REST_API_TOKEN=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ============================================================================
# Tiara Connect SMS/OTP (Meliora Technologies) (REQUIRED)
# ============================================================================
TIARA_API_KEY=your-tiara-api-key
TIARA_SENDER_ID=CONNECT

# ============================================================================
# Google Maps API (REQUIRED — plot boundary mapping, satellite layers)
# ============================================================================
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# ============================================================================
# LipaChat WhatsApp API (REQUIRED)
# ============================================================================
LIPACHAT_API_KEY=your-lipachat-api-key

# Verifies the X-Hub-Signature-256 header on inbound webhooks
# (app/api/webhooks/whatsapp/route.ts). This is your LipaChat App Secret.
LIPACHAT_WEBHOOK_SECRET=your-webhook-secret

# NOTE: lib/lipachat.ts reads LIPACHAT_WHATSAPP_NUMBER (not
# LIPACHAT_PHONE_NUMBER — that was the wrong name previously documented here
# and silently did nothing).
LIPACHAT_WHATSAPP_NUMBER=+254XXXXXXXXX

# ============================================================================
# OpenAI API (REQUIRED — AI intent processing / diagnostics)
# ============================================================================
OPENAI_API_KEY=sk-your-openai-key

# ============================================================================
# Agromonitoring API (REQUIRED — satellite imagery / EWS)
# ============================================================================
AGROMONITORING_API_KEY=your-agromonitoring-key

# ============================================================================
# Safaricom Daraja M-Pesa (REQUIRED for payments)
# ============================================================================
# lib/daraja.ts is the live, imported M-Pesa implementation. The old
# lib/mpesa.ts (and its MPESA_* vars) was unused dead code and has been
# removed — if you have MPESA_* vars set anywhere (Vercel env, local .env),
# they can be deleted along with this file's old block.
DARAJA_CONSUMER_KEY=your-daraja-consumer-key
DARAJA_CONSUMER_SECRET=your-daraja-consumer-secret
DARAJA_SHORTCODE=174379
DARAJA_PASSKEY=your-daraja-passkey
# 'sandbox' or 'production'
DARAJA_ENVIRONMENT=sandbox

# ============================================================================
# Email (OPTIONAL — not yet wired into any route)
# ============================================================================
RESEND_API_KEY=your-resend-key
EMAIL_FROM=notifications@framedinsight.com
